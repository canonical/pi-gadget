device-tree: bcm2709-rpi-2-b
volumes:
  pi2:
    schema: mbr
    bootloader: u-boot
    structure:
      - name: system-boot
        type: 0C
        filesystem: vfat
        filesystem-label: system-boot
        size: 128M
        content:
          - source: boot-assets/
            target: /
